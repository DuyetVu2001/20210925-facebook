import React from 'react';
import { View } from 'react-native';
import CreatePost from '../components/CreatePost';

const Home = () => {
	return (
		<View>
			<CreatePost />
		</View>
	);
};

export default Home;
